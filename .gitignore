# Quantumult X local bootstrap and MitM secrets
quantumultx/bootstrap.conf
quantumultx/*.p12
quantumultx/*.pem
quantumultx/*.crt
quantumultx/*.key
quantumultx/*.cer

# Clash runtime caches
clash/cache/
/cache/

# ShellCrash device-local secrets
clash/shellcrash/providers.env
clash/shellcrash/*.local.env

# Old config backups (kept locally for reference)
old/

# Codex CLI artifacts
.codex

# Claude Code local settings
.claude/
