# 配置优化实施计划

> 状态：阶段 P–H 已完成并合并；阶段 I 已完成首次运行验收，等待路由器重启与真实回滚验收
> 基线：`main` @ `112810f`
> 当前实施基线：`main` @ `b892f38`
> 原则：审核通过前不修改 Clash、ShellCrash、Quantumult X 的实际配置。

## 1. 目标

1. 让 Windows Clash / Mihomo 明确支持单订阅和双订阅，且不在 Git 中保存真实订阅地址。
2. 简化节点地区分组，保留常用地区的独立选项，修正现有跨洲误分类。
3. 删除重复策略组和重叠过高的远程规则，修复失效或格式错误的 rule-provider。
4. 将 Steam 进程、平台域名和下载 CDN 与其他游戏平台收敛到 `🎮 游戏平台`，由用户在下载时临时切换 `DIRECT`，其他时候可选代理节点。
5. 统一 Windows 端 DNS、IPv6、TUN 和局域网连接的所有权，避免 Clash Verge 界面与 YAML 相互覆盖。
6. 在 ShellCrash 接管全网后保留域名级去广告，同时保留 iOS Quantumult X 的 MitM/重写/脚本能力。
7. 用轻量、可定时执行的方式监测仓库实际引用的远程资源，不在日常检查中下载整个上游仓库。

## 2. 已确认的设计决定

### 2.1 订阅与配置所有权

- 真实订阅 URL 只存在客户端或路由器私密文件中，不提交到 Git。
- `clash/config.yaml` 继续作为完整 Windows 配置和各生成物的策略源。
- ShellCrash 继续拥有端口、DNS、TUN、sniffer、controller 和防火墙；仓库路由器模板只提供订阅、策略组和分流规则。
- 单/双订阅必须共用同一份策略源，不手工维护两套容易漂移的大配置。

### 2.2 地区分组

保留以下独立常用组：

- 🇭🇰 香港
- 🇹🇼 台湾
- 🇯🇵 日本
- 🇰🇷 韩国
- 🇸🇬 新加坡
- 🇺🇸 美国

其他节点改为：

- 🌏 东南亚：马来西亚、泰国、越南、菲律宾、印尼、柬埔寨、缅甸、老挝、文莱、东帝汶等，不再混入南亚。
- 🌏 亚洲其他：南亚、中东、中亚、蒙古、澳门及其他未独立的亚洲地区。
- 🇪🇺 欧洲：包含英国，不再单设英国组。
- 🌎 美洲：美国保持独立；加拿大、墨西哥、中美洲、加勒比和南美洲合并为一组。
- 🌏 大洋洲：合并澳大利亚、新西兰和太平洋岛国。
- 🌍 非洲：保留洲级组。
- 跨洲或海洋岛屿按实际地理与最近地区归类，不为低频岛屿新建独立组。
- 南极标签不再单设展示组，但节点仍会出现在“手动切换/自动选择”这类全节点组中，不做错误地理归类。

### 2.3 分流与去广告边界

- `linux.do` 继续归入“👨‍💻 开发服务”；将 `v2ex.com` 也加入该组。
- 路由器保留域名/连接级去广告和恶意域名拦截。
- ShellCrash/Mihomo 不冒充 QX 的 HTTPS MitM、重写和脚本能力。
- iOS 后续可继续启用 QX，由 QX 负责本机净化，而实际 `DIRECT` 流量交给路由器决定出口，避免代理套代理。

## 3. 已发现问题（实施前将再复验）

1. `Malware` 当前 URL 返回 404。
2. `Privacy` 实际引用 Loyalsoldier `private.txt`，含义是私有/内网域名，却被送到“安全防护/REJECT”，属于误分类。
3. Loyalsoldier 的 `reject.txt`、`private.txt`、`direct.txt`、`proxy.txt`、`lancidr.txt` 实际是 YAML payload，当前部分 provider 却声明为 `format: text`。
4. `Tracking` 与 `AdGuard` 高度重叠，重复维护意义很低。
5. `Epic`/`PlayStation`/`Xbox`/`Nintendo`/`BattleNet` 和 Steam 均已被 `Game` 规则包含；独立 `Steam` provider 与它高度重叠，且缺少 `steamserver.net` 等 `Game` 已有域名，会使 Steam 流量分散到两个策略组。
6. `🔄 备用线路` 只是在自动选择、故障转移和直连之间再包一层，与 `🌐 故障转移` 职责重叠。
7. 现有聚合 AI 规则包含 Cloudflare、AWS、Stripe、Google APIs 等共享基础设施域名，可能将大量非 AI 流量误送至 AI 策略。
8. Windows Clash Verge 的“DNS 覆写”和“IPv6”开关会改写运行时配置，与仓库 YAML 形成双重真值源。
9. Windows 当前只有双订阅完整配置；路由器已具备单/双订阅模板。

## 4. 实施步骤

实际执行顺序为 **P → C1 → A → B → C2 → D → E → F → G → H → I**。P 是将审核通过的本计划先独立提交留档；C1 只包含已复核的确定性 bug，将独立实施、验证和提交；C2 的规则去重与来源调整属于行为性优化，不与 bugfix 混合。地区分组放在单订阅生成能力之后，便于对单/双配置同时验证且可单独回滚。I 是对 `shellcrash方案与讨论.md` 和本计划做最终验收与归档。

### 全阶段闭环规则

每一个阶段都按以下顺序完成，不把文档统一拖到全部开发结束后再补：

1. 从当前已验证基线实施该阶段的最小改动。
2. 先运行与改动直接相关的定向测试，再运行生成、语法、引用完整性和最小冒烟验证。
3. 只有测试通过后，才更新该阶段对应的 README、ShellCrash 说明、用户操作步骤和计划验收状态；文档必须反映已验证的行为，不将预期写成已实现。
4. 文档更新后再运行该阶段的完整验收门禁，确认配置、测试和文档相互一致。
5. 完成一个可独立回滚的提交或 PR 后，才进入下一阶段。任一验证失败都停留在当前阶段修复，不靠后续阶段掩盖问题。

### 阶段 A：Windows 单/双订阅

Windows 单订阅不是预备性开发：用户已明确要求 Windows Clash 同时支持“只填一个订阅”和“填两个订阅”两种使用方式，因此保留本阶段。

1. 保留 `clash/config.yaml` 为双订阅完整配置和策略源。
2. 扩展现有生成逻辑，从同一策略源生成 `clash/config-single.yaml`：
   - 删除 `Sub2`；
   - 删除所有 `use` 中的 `Sub2`；
   - 校验所有 provider、策略组和 rule-provider 引用完整。
3. 两份公开文件只放占位 URL。README 分别给出单订阅和双订阅的下载、本地替换、Clash Verge 导入步骤。
4. 明确说明：GitHub Raw 上的公开完整配置不能直接代替私密订阅 URL；未引入私密后端前，Windows 首次仍以本地文件导入为准。

### 阶段 B：地区组和策略组精简

1. 按 2.2 重写节点名称匹配表，中英文国名、常见缩写和旗帜均覆盖，并避免 `IN`、`US` 等短缩写误匹配普通单词。
2. 保持现有常用组的选路类型，本轮只调整分类、名称和引用，不额外改变手动/自动偏好。
3. 删除 `🔄 备用线路`，原引用统一指向 `🌐 故障转移`。
4. 执行静态引用检查，确保删除旧地区组后没有悬空策略名。检查范围不仅包含 `clash/config.yaml` 内部引用，还必须覆盖 `rules/local_rules.yaml` 的 `clash_policy` / `qx_policy` 映射以及生成后的 QX snippet。

### 阶段 C：远程规则修复与去重

**C1：确定性 bugfix（首个独立 PR）**

1. 将 `Privacy` 重命名为表意准确的私有域名 provider，策略改为 `DIRECT`，且排在广告/代理规则之前。
2. 修正五个 Loyalsoldier provider 的 YAML 格式声明。
3. 用当时可用、专用于恶意域名的上游替换 404 的 `Malware`；不使用用途不同的列表冒名替换。
- 同一 bugfix PR 内将 QX 已返回 404 的 `Slack.png` 替换为已验证的 `Discord.png`。

**C2：规则去重与来源优化（行为性改动）**

4. 删除与 `AdGuard` 高度重叠的 `Tracking`。
5. 删除已被 `Game` 覆盖的 `Epic`/`PlayStation`/`Xbox`/`Nintendo`/`BattleNet`/`Steam` provider；保留 `Game`、`GameExtra` 和 Steam 下载 CDN 补充规则。
6. 将粗粒度 AI 聚合列表替换为主要 AI 服务的粒度更细且可审计的 provider，继续用 `rules/ai_extra.yaml` 补充，不把通用 CDN、支付和云域名整体送入 AI 策略。
7. 在 `rules/dev_extra.yaml` 增加 `v2ex.com`。
8. 保留墨鱼现有去广告覆盖；不盲目引入会放行统计/归因域名的 `Unbreak.list`。
9. 将墨鱼 `StartUpAds` 和 `XiaoHongShuAds` 的 parser 选项与当期官方入口对齐；不在本轮无理由替换已经可用的哔哩哔哩来源。

### 阶段 D：游戏平台统一策略

1. Steam、Epic、Xbox、PlayStation、Nintendo 和 Battle.net 统一使用 `🎮 游戏平台`，不再单设 `🎮 Steam`。
2. 保留拆分后的 `rules/steam_download.yaml`，将 21 条 Steam CDN 补充规则在 Clash 和 QX 中都指向 `🎮 游戏平台`；Microsoft、npm 等通用下载规则继续 `DIRECT`。
3. 删除与 `Game` 高度重叠且覆盖不完整的 Steam 独立 provider，Steam 平台域名由更完整的 `Game` 聚合 provider 接管。
4. Windows 侧的 `steam.exe`、`steamwebhelper.exe` 和 `steamservice.exe` 进程规则统一指向 `🎮 游戏平台`。
5. 平时给游戏平台选择合适代理；Steam 下载时临时切换 `DIRECT`，完成后切回。切换会同时影响其他游戏平台，该取舍已经用户确认。
6. 平台边界写入 README：ShellCrash 无法看到局域网客户端的进程名，依赖 `Game` 和 Steam CDN 域名规则；Windows 进程规则也只能管理**已经进入 Mihomo 核心**的流量。

### 阶段 E：Windows Clash Verge 运行时设置

YAML 目标值：

| 项目 | 目标 | 说明 |
|---|---:|---|
| `tun.enable` | `false` | 保持现状，不在本轮强制 TUN |
| `allow-lan` | `false` | WSL 已确认为 mirrored networking，可经 `127.0.0.1:7897` 访问 Windows 代理，无需对局域网开放 |
| `bind-address` | 删除显式配置 | 不再绑定 `"*"`，配合 `allow-lan: false` 缩小暴露面 |
| `external-controller` | `127.0.0.1:9090` | 继续禁止局域网访问控制器 |
| `dns.enable` | `true` | DNS 由 YAML 管理 |
| `dns.enhanced-mode` | `fake-ip` | 保持现状 |
| `ipv6` / `dns.ipv6` | `false` / `false` | 家庭原生 IPv6 已证实可用，但节点侧 IPv6 质量未知；Windows Clash 仍一起关闭两项，避免半开状态 |
| `unified-delay` | `true` | 与当前 Clash Verge 开关保持一致 |

Clash Verge 操作说明将写入 README：

- “DNS 覆写”关闭，避免覆盖 YAML 的 `dns:`。
- “IPv6”关闭，与 YAML 默认值一致。
- “局域网连接”关闭，与 `allow-lan: false` 一致；WSL 继续使用 `127.0.0.1:7897`。
- 系统代理、开机自启、静默启动和统一延迟可保持开启。

### 阶段 F：QX 与路由器共存

1. 本轮不删除 QX 的 MitM、rewrite 或脚本资源。
2. README 增加“路由器接管模式”：
   - QX 仍在 iPhone 本机运行；
   - 需要净化的请求继续由 QX MitM/重写/脚本处理；
   - 不需要 QX 节点出口时，策略选择 `DIRECT`，流量到达网关后再由 ShellCrash 分流。
3. 不在本轮自动生成第二份完整 QX 配置，先避免复制整套远程资源和形成双重真值源。
4. Windows 网页去广告定位为“路由器域名拦截 + 浏览器内容拦截”，不宣称 Mihomo 具备 QX 级 HTTPS 重写能力。

### 阶段 G：远程资源检查

1. 新增 `scripts/check_remote_resources.py`，只扫描仓库实际引用的 URL，包括 QX 的 `profile_img_url` 和 `img-url` 图标引用。
2. 默认“轻量模式”：
   - 限制并发数和单请求超时；
   - 允许合理重定向；
   - 优先使用 HTTP 状态、`Content-Length`、`Content-Type` 和前几 KB 内容判断资源是否存在、是否返回 HTML 错误页、是否具备期望的 YAML/规则头。
3. 只有疑似异常、无法使用 Range 或需要完整语法验证的资源才进入“完整模式”；不 clone 上游仓库。
4. GitHub Actions 每周执行轻量模式，手工 `workflow_dispatch` 可选完整模式。
5. 检查失败时输出资源标签、引用文件、状态码和失败类型，但不输出可能含 token 的查询参数。
6. 图标引用除检查 HTTP 2xx 外，还必须验证最终 `Content-Type` 为 `image/*`，避免 CDN 返回状态正常的 HTML/文本错误页。

### 阶段 H：同步生成物与文档

1. 运行共享规则生成器，同步 `rules/` 到 `clash/rulesets/` 和 `quantumultx/filter_remote.snippet`。
2. 重新生成 ShellCrash 单/双订阅策略模板，确保与 Windows 策略一致。
3. 对照各阶段已经在测试后同步的文档，做一次最终交叉检查：Windows 单/双订阅导入、Clash Verge 开关、ShellCrash 首次部署、IPv6 风险和 QX 共存说明必须与已验证配置一致。
4. 不改动路由器上已下载的 `providers.env` 和部署脚本；新版仓库合并后，实际部署时只需重新下载并执行脚本，无需删除私密目录。

### 阶段 I：ShellCrash 讨论文档验收与最终归档

1. 对 `shellcrash方案与讨论.md` 逐项验收，不以“有代码或有 README”代替运行证据。验收矩阵至少覆盖：
   - 公共策略、平台差异和设备私密参数的所有权是否清晰；
   - 单/双订阅模板、本地 `providers.env` 注入与秘密扫描是否完整；
   - 生成器、ShellCrash 官方覆写、Mihomo `-t`、事务化替换、备份、回滚和幂等测试是否通过；
   - ShellCrash 与仓库对 DNS、TUN、sniffer、controller、防火墙与更新任务的责任边界是否与实际一致；
   - 全新安装内核引导、已有配置保护、米家兼容、IPv4/IPv6 出口、启动和路由器重启验收是否有真实结果；
   - README 的首次部署、定时更新、故障诊断和回滚命令是否能从干净环境执行。
   - 讨论文档中未采用的 `base/` + `platforms/` + `groups/` + `generated/` 全目录重构，必须明确标记为已被现有“`clash/config.yaml` 单一策略源 + 生成路由器模板”取代，不能继续作为待执行步骤；
   - 讨论文档中“路由器默认删除 AdBlock、使用 lightweight 规则、预置广泛米家域名”等假设，必须与实际资源、性能和日志验证结果对照，未实施的不得留在操作指南中；
   - 讨论文档中的旧文件名、占位符、“自定义配置文件”接入流程，必须与当前 `config-router*.template.yaml`、本地注入脚本和 ShellCrash 官方覆写流程逐项核对。
2. 将每条结论标记为“已实现且已验证”、“被当前架构替代”、“仍待完成”或“已过时/错误”。只要仍有待完成的可执行项，就不删除该文档，而是将其精简为明确的未完成清单。
3. 当所有可执行项都完成后再决定处置：
   - 若仍有 README、实施计划和 Git 历史未保留的独特设计依据，就删除过时大段内容，修订为简短、已验证的 ShellCrash 设计决策记录并提交留档；
   - 若内容已被 README、本计划与实现完全覆盖，且继续保留只会提供过时操作，则删除该未跟踪文件。删除前必须先把必要结论和验收证据转入已跟踪文档，并向用户报告删除不可从 Git 恢复。
4. 将本文档 `配置优化实施计划.md` 作为项目记录提交留档：
   - 整体计划最终批准后，在任何实际配置改动前先做一个独立文档提交，固定审核基线；
   - 实施过程只做局部状态/验收更新，不整文件重写；
   - 全部验收完成后，更新最终状态、实际提交/验证证据和 ShellCrash 讨论文档处置结果，再做一次归档提交。

## 5. 预计修改文件

| 文件 | 计划改动 |
|---|---|
| `clash/config.yaml` | 策略组、地区组、provider、规则顺序和 Windows 运行参数 |
| `clash/config-single.yaml` | 新增 Windows 单订阅生成物 |
| `clash/config-router*.template.yaml` | 随主配置重新生成，不手工编辑 |
| `rules/dev_extra.yaml` | 增加 V2EX |
| `rules/download_extra.yaml` | 移出 Steam CDN，保留 Microsoft、npm 等通用下载直连规则 |
| `rules/steam_download.yaml` | 新增 Steam 专用下载规则源，Clash/QX 均指向 `🎮 游戏平台` |
| `rules/local_rules.yaml` | 注册 Steam 下载规则并校验 Clash/QX 策略映射 |
| `clash/rulesets/*.yaml` | 由生成器同步 |
| `quantumultx/filter_remote.snippet` | 由生成器同步共享规则 |
| `quantumultx/bootstrap.example.conf` | 调整已确认的墨鱼资源参数、地区组与失效图标 |
| `scripts/build_router_config.py` | 在不破坏现有路由器生成逻辑的前提下复用单订阅裁剪能力 |
| `scripts/check_remote_resources.py` | 新增轻量/完整两种远程检查模式，包含图标类型校验 |
| `.github/workflows/*.yml` | 加入单订阅生成物验证和每周远程资源检查 |
| `README.md` | 导入、运行时开关、去广告边界、QX/路由器共存与回滚说明 |
| `shellcrash方案与讨论.md` | 完成最终验收后，按独特历史价值与过时风险决定精简修订并归档，或在转移必要结论后删除 |
| `配置优化实施计划.md` | 实施前提交审核基线，实施后补齐验收证据和最终状态再次归档 |

## 6. 验证与验收

### 6.1 静态和生成验证

```sh
python3 scripts/build_rules.py --check
python3 scripts/build_router_config.py --check
python3 scripts/check_hygiene.py
python3 scripts/check_acceptance.py
sh -n scripts/deploy_shellcrash_config.sh
```

补充验证：

- Windows 单/双订阅配置中的所有策略组、provider 和 rule-provider 引用完整。
- 不存在已删除的地区组或 `🔄 备用线路` 残留引用。
- `rules/local_rules.yaml` 中每个 `clash_policy` 都能在 Clash 策略组或内建策略中解析，每个 `qx_policy` 都能在 QX 策略中解析；生成的 `filter_remote.snippet` 不含已删除或改名的策略。
- 单订阅配置不包含 `Sub2` 或第二个占位 URL。
- Steam 进程、`Game` 平台 provider 和专用下载规则均指向 `🎮 游戏平台`；不再存在独立 `Steam` provider 或 `🎮 Steam` 策略组。
- 没有真实订阅 URL、token、QX 证书或密码进入 Git diff。

### 6.2 Mihomo 与 ShellCrash 验证

- 用 CI 锁定的 ShellCrash Mihomo 分别对 Windows 单/双订阅配置执行 `-t`。
- 对 ShellCrash 单/双模板执行官方覆写脚本后再执行 `-t`。
- 运行：

```sh
python3 scripts/test_deploy_shellcrash.py
```

- 验证部署连续执行两次幂等，下载/注入/内核检查失败时不覆盖现有配置。

### 6.3 功能验收

Windows：

- 单订阅和双订阅分别能加载，不出现空 provider 或策略组。
- 地区节点样本能进入期望组，常见普通单词不被两字母缩写误命中。
- Clash Verge 关闭 DNS 覆写后，运行配置仍为 `fake-ip` 且 DNS 可用。
- IPv6 顶层与 DNS 值一致。
- `allow-lan: false` 且 Clash Verge 关闭局域网连接后，WSL 仍可通过 `127.0.0.1:7897` 访问 Windows 代理。
- `🎮 游戏平台` 切换为 `DIRECT` 或代理节点时，所有已进入 Mihomo 的 Steam 进程、平台域名和下载 CDN 以及其他游戏平台共用该策略；TUN 关闭时对绕过系统代理的流量不作伪保证。

路由器（等家中无人使用网络时执行）：

- 首次单订阅部署成功，ShellCrash 自行获取内核并启动。
- 常用国内、国外、Steam 下载、广告域名和米家设备通信分别冒烟测试。
- 分别测试 IPv4 和 IPv6 公网出口并对照 ShellCrash 连接日志；若 IPv6 绕过透明代理，不宣告部署通过，先在 ShellCrash/路由器本地决定接管或限制 IPv6。
- 失败时能恢复 `config.yaml.bak.proxy-config`，不把家庭网络留在无代理或无 DNS 状态。

iOS：

- QX MitM 证书与私密信息没有被公开文件覆盖。
- QX 开启时原有 rewrite/脚本去广告仍生效。
- QX 选择 `DIRECT` 时，路由器仍可对其上游流量进行透明分流。
- 所有 `profile_img_url` / `img-url` 均能跟随重定向获得 HTTP 2xx 且最终内容类型为 `image/*`。

### 6.4 文档与归档验收

- 每个阶段的文档改动都有同阶段测试结果支持，不存在已删除配置名、失效命令、旧路径或未实现能力的宣称。
- `shellcrash方案与讨论.md` 的验收矩阵不含未解决的“仍待完成”项，或者项目明确保留该文档作为未完成清单而不宣告整体完成。
- 若保留 ShellCrash 讨论文档，其修订版已被 Git 跟踪；若删除，必要结论与验收证据已进入 README 或本计划，且已向用户报告。
- `git ls-files --error-unmatch '配置优化实施计划.md'` 成功，证明本计划已提交留档；最终版记录实际提交、测试证据和 ShellCrash 文档处置结果。

## 7. 回滚策略

- 用户最终批准后，先将本计划作为独立文档提交（阶段 P），再开始配置改动，使后续实际结果可与获批基线对照。
- 首个实施 PR 只处理阶段 C1 的明确 bug；地区分组、C2 去重和其他行为性改动不与它混合。
- 仓库改动按阶段拆分为小提交，配置、远程检查和文档不混成一个难以定位的巨型提交。
- Windows 导入新配置前保留当前 Clash Verge profile。
- ShellCrash 依赖现有事务化部署和 `config.yaml.bak.proxy-config` 回滚。
- 如新的远程 provider 失效，优先回滚该 provider 的单项改动，不同时改 DNS/TUN 来掩盖错误。

## 8. 本轮不做

- 不把真实订阅、token、QX p12、passphrase 或路由器管理密钥写入仓库。
- 不在 ShellCrash 中强制覆盖 DNS、TUN、sniffer 或防火墙。
- 不部署 HTTPS 中间人代理到路由器，不尝试在路由器复制 QX 的 MitM 能力。
- 不下载或 clone 所有上游仓库文件做日常检查。
- 不在本轮强制开启 TUN 或 IPv6。
- 不引入 AdGuard Home/Pi-hole 等新的常驻服务。

## 9. 已确认的四个取舍

- [x] 将澳门归入“亚洲其他”，不再保留独立组。
- [x] 不设南极地区组，这类节点仅留在全节点组。
- [x] Steam 合并到 `🎮 游戏平台`，进程、平台域名和下载 CDN 与其他游戏平台共用一个可切换出口。
- [x] Windows 设置 `allow-lan: false`，关闭 Clash Verge 的局域网连接、DNS 覆写与 IPv6；WSL 通过 mirrored networking 和 `127.0.0.1:7897` 继续使用代理。

四项取舍已确认，并已按阶段提交、验证和合并；实际结果见第 18 节。

---

## 10. Claude 审核意见（2026-07-18）

> 结论：计划整体质量高，"已发现问题"经实际抽查全部属实，可以批准实施；但建议做以下三处调整，四个取舍项给出建议。

### 10.1 事实核查结果

以下断言经实际验证（curl 实测，非推测）：

- `Malware` 上游 URL 确实返回 404，该规则当前是坏的；
- Loyalsoldier `reject.txt` / `private.txt` 等确实是 YAML payload 格式，与 `format: text` 声明不符；
- `Privacy`（`private.txt`）内容确实是路由器管理域名、内网域名（如 `asusrouter.com`），被送进 🛡️ 安全防护属于真实误分类，会拦截自家设备管理页面；
- AI 聚合 gist 确实混有 Cloudflare / AWS / Stripe / Google APIs 等共享基础设施域名（6 处命中）；
- `🔄 备用线路` 冗余属实。

即：阶段 C 修的是真实存在的 bug，不是为优化而优化。

### 10.2 建议调整

1. **实施顺序：阶段 C 最先做，独立成一个 PR。**
   404、格式声明错误、Privacy 误分类是明确 bug，风险低、收益确定。阶段 B（地区组重构）是偏好性重组，靠正则匹配节点名，最容易出回归——应放在后面单独提交，出问题好定位好回滚。

2. **阶段 A（Windows 单订阅配置）的必要性待确认。**
   计划未说明 Windows 何种实际场景下只用单订阅。若该场景不存在，属预备性开发，按"不过度开发"原则应砍掉——路由器侧已有单/双模板。

3. **B4 静态检查补一条遗漏：**
   删除/改名策略组后，除 `config.yaml` 内部引用外，必须同时检查 `rules/local_rules.yaml` 中的 `clash_policy` / `qx_policy` 映射。那里的策略组名是字符串引用，改名后不会有工具报错，但生成的 QX snippet 和 rulesets 会指向不存在的组。建议加入 6.1 验收清单。

### 10.3 对第 9 节四个取舍的建议

四条均建议同意：

- 澳门归"亚洲其他"：澳门节点独立组意义不大；
- 不设南极组：合理；
- Steam 仅下载域名直连：进程级直连会让商店/登录也绕开代理，超出需求；
- Windows 保留 `allow-lan: true`、关闭 Verge DNS 覆写与 IPv6：等 WSL / IPv6 实测后再动，稳妥。

### 10.4 仓库整洁

根目录现有两份未跟踪方案文档（`shellcrash方案与讨论.md`、本文件），加上已提交的 `quantumultx/重新设计方案.md`（位置错放且已过时）。建议实施前建 `docs/` 目录收拢，删掉过时文档，避免方案散落。

### 10.5 待讨论问题清单

- [ ] 阶段 A 是否保留？（Windows 单订阅的实际使用场景是什么）
- [ ] 实施顺序是否调整为 C → A/B → D~H？
- [ ] `local_rules.yaml` 策略名检查是否纳入 6.1？
- [ ] 方案文档是否收拢到 `docs/`？

---

## 11. Codex 二次审核结论（2026-07-18）

### 11.1 接受的意见

1. **接受阶段 C 优先且独立实施。** `Malware` 404、Loyalsoldier 格式错误和私有域名被 REJECT 都是确定性 bug，应与偏好性的地区组重构分开。计划已改为 C → A → B → D → E → F → G → H。
2. **接受 `local_rules.yaml` 映射检查。** 这是原计划的真实漏项，已同时加入阶段 B 和 6.1 验收清单。
3. **认同四个取舍建议的技术判断。** 但 Claude 的建议不代表用户授权，第 9 节仍保持未勾选，等用户最终确认。

### 11.2 不接受或修正的意见

1. **保留阶段 A。** 用户在本轮讨论中已明确提出“Windows Clash 也要支持单、双订阅链接”，因此这是已知使用场景，不是无需求的预备开发。已在阶段 A 补写该依据。
2. **不执行当前的文档删除/搬迁建议。** 当前 `main` 及本地工作树中都不存在 Claude 所述已提交的 `quantumultx/重新设计方案.md`，`git log --all` 也无该路径记录。现有 `shellcrash方案与讨论.md` 是用户的未跟踪文件，不在未获得授权时移动或删除。本计划文档可在审核结束后再由用户决定是否纳入 `docs/`。

### 11.3 审核后状态

- 计划现已覆盖 Claude 提出的两项有效技术补充。
- 尚未修改任何 Clash、ShellCrash 或 QX 实际配置。
- 开始实施前，仍需用户确认第 9 节四个取舍并批准整体计划。

---

## 12. Claude 第二轮复核（2026-07-18）

### 12.1 结论

**无遗留技术异议，计划批准就绪。** 剩余唯一阻塞项是用户勾选第 9 节四个取舍。

### 12.2 逐条复核

1. 11.1 的三条接受项已核实真实落实到计划正文（§4 顺序、§7 回滚、B4 + 6.1 检查项），且 6.1 补充的"检查生成后的 `filter_remote.snippet`"比原建议覆盖更完整。
2. 保留阶段 A：接受。用户已明确提出 Windows 需支持单/双订阅两种方式，撤回"预备性开发"质疑。
3. 文档处置：**Claude 更正第一轮 10.4 中的事实错误**——`quantumultx/重新设计方案.md` 从未被提交（`git log --all` 无记录），且现已不在工作树中；10.4 中"已提交的"表述作废。Codex"未经授权不动用户未跟踪文件"的立场正确。文档是否收拢到 `docs/` 由用户在实施后自行决定，不阻塞本计划。

### 12.3 等待用户的决定

- [ ] 勾选第 9 节四个取舍（Claude 与 Codex 对四条的技术判断一致：均建议同意）。
- [ ] 批准整体计划，开始阶段 C。

---

## 13. 用户裁决与 Claude 实测研究（2026-07-18，第三轮）

### 13.1 用户对第 9 节四项的裁决

| 项 | 裁决 |
|---|---|
| 1. 澳门归"亚洲其他" | ✅ 同意 |
| 2. 不设南极组 | ✅ 同意 |
| 3. Steam 仅下载直连 | ❌ **否决**：Steam 整个进程统一管理，不管直连还是代理（见 13.2） |
| 4. allow-lan / DNS 覆写 / IPv6 | 交由 Claude 实测后给建议（见 13.3） |

### 13.2 取舍 3 改判后的设计（阶段 D 需按此重写）

用户意图：Steam 全部流量收敛到**一个可切换的策略组**统一管理，而不是拆成"下载强制直连 + 其余走代理"。

实施要点：

1. 新建 `🎮 Steam` select 策略组（候选：`DIRECT`、常用地区组、手动切换；默认值由用户日常习惯定，建议默认 `DIRECT`）。
2. 所有 Steam 相关规则统一指向该组：
   - Windows 侧增加 `PROCESS-NAME,steam.exe` / `PROCESS-NAME,steamwebhelper.exe` → `🎮 Steam`，置于 Game 域名规则之前；
   - 现有 `rules/download_extra.yaml` 中的 Steam CDN 规则改为指向 `🎮 Steam`（不再固定 `DIRECT`）；
   - Steam 平台域名规则同样指向该组。
3. **平台限制必须写进 README**：`PROCESS-NAME` 只在 Windows 本机 Mihomo 生效；路由器透明代理看不到远端进程，ShellCrash 上只有域名规则起作用。因此进程规则是 Windows 的补充兜底，域名规则才是两端共同的基础——两者都必须指向同一个 `🎮 Steam` 组，才能保证"一个开关管全部"在两端语义一致。
4. 需向用户明示的代价（已知晓即可，不阻塞）：组切到代理时，游戏下载会消耗代理流量（与 commit 46277f5 的初衷相反）；组切到 `DIRECT` 时，商店/社区图片可能加载失败，需要临时手动切回代理。这正是"统一开关"换来的灵活性与代价。
5. 原阶段 D 第 2 条（MetaCubeX 下载 MRS 固定 `DIRECT`）中**属于 Steam 的部分**改为指向 `🎮 Steam`；Epic 等其他平台的下载规则不受本裁决影响，维持原计划。

### 13.3 取舍 4 的实测结论与建议（Claude 在用户 WSL 内实测）

实测数据（2026-07-18，非推测）：

1. `wslinfo --networking-mode` = **mirrored**；
2. WSL 内经 `127.0.0.1:7897` 走 Windows 代理访问外网**成功**（HTTP 204），且 WSL 环境变量本就配置为 `127.0.0.1:7897`；
3. 家庭网络有可用的原生 IPv6（电信 240e::/ 前缀，WSL 直连 IPv6 站点返回 200）。

据此建议（供 Codex 修订阶段 E）：

- **`allow-lan` 可以关闭**（`false`），Clash Verge"局域网连接"开关同步关闭，`bind-address` 恢复默认。阶段 E 原表中"保留 WSL NAT 访问"的前提已被实测否定——mirrored 模式下 WSL 经环回地址即可用代理。局域网其他设备的代理需求将由路由器 ShellCrash 接管，Windows 不需要再当局域网代理出口。
- **Verge "DNS 覆写"与"IPv6"开关：同意关闭**，与 YAML `ipv6: false` / `dns.ipv6: false` 保持一致。家庭网络虽有原生 IPv6，但 Windows Clash 开启 IPv6 对分流无实际收益，且节点侧 IPv6 质量未知，维持关闭。
- **新增一条路由器阶段的风险提示**（应记入 ShellCrash 文档，非本轮实施）：家庭网络存在原生 IPv6 意味着 ShellCrash 接管后，支持 IPv6 的设备可能经 IPv6 直接绕过透明代理，造成分流不一致。路由器部署验收时必须实测 IPv6 泄漏，再决定是在 ShellCrash 中接管 IPv6 还是在路由器上限制 IPv6。

### 13.4 汇总：计划需要的修订

- [x] 阶段 D 按 13.2 重写（🎮 Steam 统一策略组）。
- [x] 阶段 E 按 13.3 修订（`allow-lan: false`、关闭 Verge 局域网开关）。
- [x] 第 9 节取舍 1、2、4 视为已确认；取舍 3 按 13.2 的新方案确认。
- [x] ShellCrash 部署验收清单增加 IPv6 泄漏实测项。

以上修订完成后，计划即可进入实施（从阶段 C 开始）。

---

## 14. 远程图标有效性审计（2026-07-18，Codex；由 Claude 从被覆盖版本恢复）

### 14.1 检查范围与结果

- 扫描仓库配置中所有显式 `profile_img_url`、`img-url` 和图片扩展名链接。
- 当前只有 `quantumultx/bootstrap.example.conf` 显式引用远程图标。
- 共有 **46 处引用、42 个唯一 URL**；其中 **35 个返回 `200 image/png`，7 个返回 404**。
- 策略名中的 emoji/旗帜是 Unicode 文字，不依赖远程图片 URL，不在 HTTP 失效范围内。

### 14.2 失效引用与有效替代

| 当前失效图标 | 当前用途 | 计划替代 | 替代验证 |
|---|---|---|---|
| `Slack.png` | 🎙️ 商务协作 | `Discord.png` | `200 image/png`；Discord 已归入该策略 |
| `Southeast_Asia.png` | 🌏 东南亚 | `Asia_Map.png` | `200 image/png` |
| `New_Zealand.png` | 🇳🇿 新西兰 | 删除独立组，合并后使用 `Oceania_Map.png` | `200 image/png` |
| `Oceania.png` | 🏝️ 大洋洲 | `Oceania_Map.png` | `200 image/png` |
| `South_America.png` | 🌎 南美洲 | `LA_Map.png` | `200 image/png`；图形为拉丁美洲轮廓，非洛杉矶 |
| `Middle_East.png` | 🕌 中东，后续为中东与中亚 | `Asia_Map.png` | `200 image/png` |
| `Africa.png` | 🌍 非洲 | `Africa_Map.png` | `200 image/png` |

地区重构新增的"北美洲"使用已验证的 `America_Map.png`；"欧洲"改用语义更准确且已验证的 `Europe_Map.png`。"亚洲其他"与"东南亚"共用 `Asia_Map.png`，避免引用 Qure 中不存在的细分地区文件名。

### 14.3 实施要求

1. 上述图标替换纳入阶段 B 的 QX 地区组同步改动；`Slack.png` 可随阶段 C 的明确失效引用一并修复。
2. 阶段 G 的远程检查脚本必须继续覆盖图标 URL（包括 `profile_img_url` / `img-url`），后续任何 404 或非 `image/*` 响应均应使每周检查失败。
3. 实施前再执行一次轻量复验，因为 `@master` 资源可能随上游变化。

---

## 15. 文档并发写入事故记录（2026-07-18）

本文档曾在第三轮讨论期间被基于过时副本的整体重写覆盖，导致 §13、§14 及阶段 G 的图标检查条目一度丢失，已由 Claude 恢复。**约定：此后对本文档只做追加或局部编辑，禁止整文件重写；Codex 修订计划正文（如按 13.4 重写阶段 D/E）时，必须基于当前最新文件内容。**

---

## 16. Claude 最终复核（2026-07-18，第四轮）

### 16.1 结论

**13.4 的四项修订已全部落实并核对，计划批准实施，从阶段 C 开始。** 本轮 Codex 采用的是基于最新文件的局部编辑，未再发生覆盖事故。

### 16.2 核对明细

- 阶段 D：已重写为 `🎮 Steam` 统一策略组，且优于 13.2 原始设计——将 Steam CDN 拆到独立的 `rules/steam_download.yaml` 而不是整体挪动 `download_extra.yaml`（避免 Microsoft、npm 等通用下载规则被 Steam 策略误控）；QX 侧映射到既有 `🎮 游戏平台` 而不新增无用策略组；补充了 `steamservice.exe`。
- 阶段 E：`allow-lan: false`、删除 `bind-address`、Verge 三开关说明齐备，与 13.3 实测结论一致。
- §9：四项取舍全部按裁决更新并勾选。
- IPv6 泄漏实测：已写入 6.3 路由器验收，且标准更严——IPv6 绕过透明代理即不判部署通过。
- 连带一致性：§1 目标 4、§5 文件清单、6.1 Steam 指向检查、阶段 G/6.3 图标校验均已同步，无残留矛盾表述。

### 16.3 一处值得肯定的诚实声明

阶段 D 第 5 条明确承认：TUN 关闭时，不遵循系统代理的 Steam 流量会绕过 Windows Mihomo，进程规则无法强制控制，需等路由器接管后由域名规则兜底。这是真实的技术边界（Steam 下载器历史上经常无视系统代理），写明它比假装"一个开关全管住"更可靠。用户应知悉：**在 Windows 上，🎮 Steam 开关的完全生效依赖后续开启 TUN 或路由器透明代理。**

### 16.4 状态

- 计划：✅ 双方审核通过，等待用户一句"开始实施"。
- 建议实施节奏：阶段 C 单独 PR → 验证 → 合并，再进入 A/B。

---

## 17. 用户最终批准（2026-07-18）

用户已明确要求按本计划的目标与阶段开始执行并持续至完成。按阶段 P 先将本计划提交留档，随后进入 C1。

---

## 18. 实施进度与证据（2026-07-18）

### 18.1 已完成并合并

| 阶段 | PR | 结果 |
|---|---:|---|
| P | #9 | 审核通过的实施计划独立提交留档 |
| C1 | #10 | 修复私有域名误分类、Loyalsoldier 格式、Malware 来源和失效图标 |
| A | #11 | Windows 单订阅配置由双订阅策略源生成，补齐导入说明和验收检查 |
| B | #12 | 完成首轮 14 个地区组、删除冗余组并加入地区正反样本测试；后续按 §19 再收敛为 12 组 |
| C2 | #13 | 删除高重叠 provider，细化 AI 来源，补齐 V2EX/Linux.do 语义 |
| D | #14 | 首轮将 Steam 进程、平台域名和 21 个下载 CDN 统一进入 `🎮 Steam`；后续按 §19 合并至 `🎮 游戏平台` |
| E/F | #15 | 固化 Windows DNS/IPv6/TUN/LAN 默认值，补齐 QX 共存与去广告边界 |
| G | #16 | 增加每周远程资源巡检；181 个唯一依赖实测通过，移除 4 条返回 HTML 首页的失效墨鱼引用 |
| H | #17 | 重新生成无差异，修正文档中的配置所有权和规则单源表述 |

### 18.2 已获得的验证证据

- 共享规则、Windows 单订阅和 ShellCrash 单/双模板生成一致性检查通过。
- hygiene、acceptance、地区、Steam、远程资源离线测试和 ShellCrash 部署事务测试通过。
- Windows 单/双配置与路由器单/双模板均通过 CI 锁定的 ShellCrash Mihomo `-t`。
- 路由器单/双模板经过 ShellCrash 官方覆写脚本后再次通过 Mihomo `-t`。
- WSL2 mirrored networking 经 `127.0.0.1:7897` 使用 Windows 代理访问 `generate_204` 返回 204。
- 远程轻量巡检覆盖 70 个 Clash 规则、37 个图标、67 个 QX 资源、5 个脚本和 2 个 ShellCrash 模板，最终 181 项全部通过。
- PR #9–#17 的 GitHub `audit`、`hygiene` 和 `validate` 检查均通过后合并。

### 18.3 阶段 I 当前状态

`shellcrash方案与讨论.md` 已基于当前文件局部增加验收矩阵，并明确将旧目录重构、lightweight/默认删 AdBlock、广泛米家清单和旧“自定义配置文件”路径分类为“被替代”或“已过时”。该文档暂不删除，因为以下设备证据仍缺失：

- [x] 目标路由器首次单订阅部署，ShellCrash 自动取得核心并启动服务。
- [x] 国内、国外、Steam、广告和米家实际通信及连接日志已验收。
- [x] IPv4/IPv6 出口已检查，未发现未处置的本地 IPv6 直连绕行。
- [ ] 路由器重启后自动恢复服务。
- [ ] 可控失败回滚演练验证 `config.yaml.bak.proxy-config`。

在这些条目完成前，本计划不标记为“全部完成”，讨论文档保留为待验收清单。完成后再局部更新本节的最终结果、讨论文档处置决定和最终提交证据。

---

## 19. 真实运行后的策略收敛（2026-07-19）

路由器连接日志显示 `steamserver.net` 因独立 `Steam` provider 覆盖不完而落入 `Game` provider。结合用户的实际操作习惯，用户确认以下变更：

1. 删除 `🎮 Steam` 策略组和独立 `Steam` provider；Steam 进程、`Game` 平台域名和 21 条下载 CDN 补充规则全部指向 `🎮 游戏平台`。平时可走代理，Steam 下载时手动切换 `DIRECT`。
2. 中东、中亚合并至 `🌏 亚洲其他节点`；美国继续独立，加拿大、墨西哥、中美洲、加勒比和南美洲合并至 `🌎 美洲节点`，地区组由 14 个收敛为 12 个。
3. 以上变更同步到 Windows 单/双订阅、ShellCrash 单/双模板、QX bootstrap、共享规则映射、README 和 ShellCrash 验收记录。
4. 验证已通过：12 个地区组的 20 个正样本与 4 个反样本全部通过；Steam 的 3 个进程规则、21 条 CDN 和 `Game` provider 均指向 `🎮 游戏平台`，且不存在独立 Steam 组/provider 残留；生成一致性、hygiene、acceptance、部署事务、ShellCrash 官方覆写和四份 Mihomo `-t` 全部通过；179 个当前远程依赖（含 36 个唯一图标）在线轻量巡检全部通过。

本节是用户基于真实运行结果做出的后续裁决，取代 §13、§16 和 PR #14 中“保留独立 `🎮 Steam`”的旧结论；旧段落仅作为决策演变记录保留。
